interface:
  display_name: "Learning Route"
  short_description: "Turn learning intent into the next useful action"
  default_prompt: "Use $learning-route to handle my learning intent in plain language. Infer the right action, keep workflow state internal, and continue into teaching or synthesis when possible."
policy:
  allow_implicit_invocation: true
