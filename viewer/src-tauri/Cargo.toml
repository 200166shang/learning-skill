[package]
name = "learning-companion"
version = "0.1.0"
description = "Read-only desktop view for the Learning runtime"
edition = "2021"

[lib]
name = "learning_companion_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde_json = "1"
tauri = { version = "2", features = [] }
