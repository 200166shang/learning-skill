interface:
  display_name: "Learning: Practice"
  short_description: "Apply saved understanding in one concrete task"
  default_prompt: "Use $learning-practice to turn this existing Learning Thread into one concrete application task, let me attempt it before revealing the solution, and explain any mechanism-level gap in my answer."
policy:
  allow_implicit_invocation: false
