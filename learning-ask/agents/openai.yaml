interface:
  display_name: "Learning: Ask"
  short_description: "Find the right Learning Suite path and current next step"
  default_prompt: "Use $learning-ask to tell me what the Learning Suite can do, where I currently am, and which Learning skill I should use next."
policy:
  allow_implicit_invocation: false
